import React from 'react';

function createUseContext(contextProviderWrapperCreator) {
  var Context = React.createContext();

  var useContext = function useContext() {
    return React.useContext(Context);
  };

  var ContextProvider;

  if (typeof contextProviderWrapperCreator === 'function') {
    ContextProvider = contextProviderWrapperCreator(Context.Provider);
  } else {
    ContextProvider = Context.Provider;
  }

  return {
    Context: Context,
    ContextProvider: ContextProvider,
    ContextConsumer: Context.Consumer,
    useContext: useContext
  };
}

export default createUseContext;
